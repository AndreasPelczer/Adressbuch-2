<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.loginapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginApp"
        tools:targetApi="31">

        <!-- The com.example.loginapp.MainActivity as the start activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.loginapp.HomeActivity"
            android:exported="true"
            tools:ignore="MissingClass" />

        <!-- Add the com.example.loginapp.ProfileActivity -->
        <activity
            android:name="com.example.loginapp.UserProfil"
            android:label="Profile"
            tools:ignore="Instantiatable" />

    </application>

</manifest>
